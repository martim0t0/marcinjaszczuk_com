import type { Project } from "@/types/resume";

// No projects in the source resume yet — the Projects accordion hides
// itself on the resume page while this stays empty.
export const projects: Project[] = [];
